# Dependencies and generated frontend files
/node_modules/
/dist/
/.flatpak-builder/
/build-flatpak/
/flatpak-repo/
/*.flatpak
*.tsbuildinfo
/vite.config.js
/vite.config.d.ts

# Rust/Tauri build output and generated capability schemas
/src-tauri/target/
/src-tauri/gen/

# Built executables, packages, and atomic build staging files
/inpaint-desktop
/.inpaint-desktop.*
/*.AppImage
/*.deb
/*.rpm
/inpaint-extension.crx
/inpaint-extension.zip

# Local Python runtime and caches
/.venv/
/venv/
/runtime-tools/
/python/
/.runtime-managed
/.runtime-ready
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.cache/
packaging/
# Downloaded weights, generated worker scripts, and Hugging Face credentials
/models/**
!/models/README.md

# Local credentials/configuration; example environment files may be committed
# App data written to the data directory, which is the checkout when it has .venv/.
# inpaint.db holds credentials; the JSON files are from before it and are imported on start.
/inpaint.db
/inpaint.db-wal
/inpaint.db-shm
/server.json
/immich.json
/stores.json
/dropbox.json
/tray.json
.env
.env.*
!.env.example
!.env.sample
!.env.template
.idea/
*.log
.DS_Store
Thumbs.db
*.crx

# resources/uv.gz and resources/*.whl are required embedded build inputs.

